# Google OAuth — required for "Continue with Google" button
# Create a Client ID at https://console.cloud.google.com/apis/credentials
# Add http://localhost:3000 and your Vercel URL to "Authorized JavaScript origins"
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
